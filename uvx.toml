# This file configures the 'web-forager' command for UVX.
# All project metadata, dependencies, and build configurations are in pyproject.toml.

[tool.uvx.tool.web-forager]
entry_point = "web_forager.cli:main"
